#include<stdio.h>
#include <string.h>
void main(){
	
	  char str[]={"Ashutosh"};
	  int s=strlen(str);
	  printf("this is a length of string = %d",s);
	  // strlen funcion is used to calculet the no of character in a string
	  //number of characters before the null terminator \0).
}